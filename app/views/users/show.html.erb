<h3 class="text-center mt-4">Welcome to  <%= @user.username %>'s page</h3>

<div class="row">
  <div class="col-4 offset-4 text-center">
    <%= gravatar_for @user, size: 150 %>
  </div>
</div>

<h5 class="text-center mt-4">Showing <%= @user.username %>'s Articles</h5>

<%= render 'articles/article', obj: @user.articles %>

